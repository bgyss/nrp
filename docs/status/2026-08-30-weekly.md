# Weekly Status Report — Week Ending 2026-08-30

Scope: commits dated 2026-08-24 through 2026-08-30 (America/Los_Angeles)
reachable from every local branch and current remote-tracking ref, all registered
local worktrees, and committed experiment artifacts. Inspected on `main` at
`28e77bf`. This report preserves the existing branch topology; it does not merge
feature branches.

## Week in Brief

The representation track substantially improved its experimental controls, but
its primary promotion outcome remains unchanged. The team:

- completed the R1A variance decomposition, the R1 coordinate/independent-scene
  promotion audit, and an opt-in R2 camera-conditioned pilot;
- implemented fair-allocation Kitchen parity, a three-outcome equivalence gate,
  and a held-out-light re-scorer;
- discovered that the original 12-light estimator was noisier than the Kitchen
  parity margin, then re-read committed checkpoints at 96 lights without
  retraining; and
- ran the next scheduled 32-seed Kitchen parity look. `world_sparse` passes
  that **single-view fair-allocation equivalence gate** (mean −0.096 dB; 99.17%
  CI [−0.447, +0.254] dB), while `world_normal_triplane` and `world3d` remain
  `continue`.

This last pass does not reopen R1. The binding held-out-camera generalization
campaign remains a characterized negative: at 96 held-out lights, no arm passes
G1/G3/G4 and its failure count rose from 83 to 94 of 180 rows.

## Work Delivered

### Representation experiments and promotion controls

- The R1A five-seed variance decomposition added
[`examples/r1a_variance.py`](../../examples/r1a_variance.py), its test, and
committed reports. The target-scale world tri-plane was a candidate on the
original Kitchen seeds, but the result was not promoted.
- The R1 promotion audit added world-anchored encoder variants, occupancy
instrumentation, coordinate-frame gates, reproducible reports, and tests. The
corrected R1C/R1E evidence contains binding coordinate-robustness and
independent-scene failures, so R1 stays closed and R2–R6 remain blocked.
- The R2 camera-conditioned implementation and pilot were added as an opt-in
path. Its three-view deltas were negative, and it does not satisfy the R1
prerequisite.

Primary changes span `nrp/torch_backend/{encoding,encoder_registry,occupancy,
sparse_encoding,conditioned_multiview,train}.py`, `examples/r1_encoding_redesign.py`,
`examples/r1_promotion.py`, `examples/r2_conditioned.py`, and their focused tests.
The authoritative evidence remains
[`out/r1-encoding-redesign/report.json`](../../out/r1-encoding-redesign/report.json),
[`out/r1-promotion/report.json`](../../out/r1-promotion/report.json), and
[`out/r2-conditioned/report.json`](../../out/r2-conditioned/report.json).

### Equivalence gate and Kitchen parity

- Replaced the prior per-seed Kitchen-parity decision rule with the scheduled,
  three-outcome equivalence gate in
[`nrp/experiment_gate.py`](../../nrp/experiment_gate.py), wired it into
[`examples/r1_parity.py`](../../examples/r1_parity.py), and added simulation and
  call-site tests. The deterministic OIDN fix is part of this control work.
- Added the K1 finest-resolution sweep and its gate binding. Its direction is
  still underpowered/undecided, so K2–K4 remain cancelled.
- Decoupled promotion-gate light count from training validation, made 96 the
  configured gate-light default, and added
[`examples/rescore_checkpoints.py`](../../examples/rescore_checkpoints.py) with
reproduction and input-validation coverage. A 12-light re-score reproduces the
committed parity values before the 96-light reads are used.
- Re-read the R1 Kitchen parity, K1 sweep, and encoding-redesign artifacts at
 96 held-out lights without retraining. The new precision changed no existing
 verdict: Kitchen arms were `continue`, K1 remained undecided, and the binding
 R1 negative strengthened.
- At the scheduled 32-seed Kitchen look, `world_sparse` passes the
 fair-allocation equivalence gate; its result is recorded in
 [`out/r1-parity-kitchen-eq32/report.json`](../../out/r1-parity-kitchen-eq32/report.json).
 Multiplicity across the three arms is explicitly uncorrected, and this is not a
 held-out-camera generalization result.

The corresponding decision record is
[`docs/status/2026-08-29.md`](2026-08-29.md); detailed methodology, corrections,
and results are in [`docs/performance.md`](../performance.md) and
[`docs/representation-track.md`](../representation-track.md).

## Commit, Branch, and Worktree Evidence

| branch / worktree | commits in window | evidence and current state |
|---|---|---|
| `main` / repository root | Merged R1A, encoding-redesign, K1/equivalence-gate, and held-out-light-estimator work; then recorded the 32-seed parity look. | `28e77bf` is the latest local commit. Before this report, `main` is 17 commits ahead of `origin/main`; those commits include the already-local merges and the final parity artifact. |
| `codex/run-r1a-paired-experiment` / detached worktree at `3c2c2fc` | R1A evidence and documentation (Aug 25). | Local branch still names the completed R1A tip; its remote-tracking ref was pruned after fetch because the remote branch was deleted. |
| `codex/goal-implement-r2` / detached worktree at `a980d72` | Promotion-audit correction and closure (Aug 26). | Local and `origin/codex/goal-implement-r2` both point to `a980d72`. |
| `representation-encoding-redesign` | R1 redesign, fair-allocation parity, R2 pilot, and promotion-audit work merged to `main` (Aug 25–27). | Local ref remains at merge-base commit `85548cb`; its remote-tracking ref was pruned after fetch because the remote branch was deleted. |
| `k1-equivalence-gate-rerun`, `heldout-light-estimator` | K1 gate re-run, 96-light estimator repair/re-reads, documentation corrections, and tests (Aug 28–29). | Their completed work is merged into local `main`; the K1 merge is `ddec973` on `origin/main`, while held-out-light work is among local `main`'s 17 unpublished commits. |

`git log --all` constrained to the reporting window, `git for-each-ref`,
`git worktree list --porcelain`, and a post-fetch ref inspection were used for this
table. The registered detached worktrees are clean. The root worktree has one
unrelated uncommitted metadata-only change in `out/pipeline-feasibility/audit.json`
(a machine-local source path was updated); it is intentionally excluded from this
report commit.

## Verification Evidence

The report does not claim a new full test run. The committed held-out-light
estimator review records its verification: `tests.test_rescore_checkpoints` (5
tests), the complete suite (681 tests, 4 optional-dependency skips), and Ruff were
clean; the pipeline-feasibility audit had only the pre-existing missing
`out/r1-promotion/bedroom_cache.npz` input. The 32-seed commit additionally records
seeds 0–7 reproducing the earlier deltas to 1e-9 in fresh training processes.

Focused coverage added this week includes gate power and boundary tests,
`r1_parity`/K1 call-site wiring, checkpoint re-scoring reproduction,
camera/encoding/occupancy behavior, promotion audit regressions, and R1A variance
reporting. Experiment outputs are committed alongside the code rather than inferred
from prose.

## Remaining Work

1. Treat the 32-seed `world_sparse` result as narrowly scoped. Do not promote R1
   or advance R2–R6 unless the declared held-out-camera generalization gate is
   changed by an approved plan and then satisfied.
2. Resolve the two Kitchen `continue` arms only at a scheduled equivalence-gate
   look if further CPU spend is authorized; preserve the 96-light estimator and
   uncorrected-multiplicity caveat.
3. Keep K2–K4 cancelled unless K1 first establishes its predicted resolution
   direction. Maintain the corrected estimator/reproduction controls for any new
   representation-track experiment.

For the preceding weekly baseline, see
[`2026-08-23-weekly.md`](2026-08-23-weekly.md).
