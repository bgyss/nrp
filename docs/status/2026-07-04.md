# Status Report - 2026-07-04

Scope: all local branches and worktrees visible from this checkout. This report is
based on local git history, branch/worktree state, existing docs, and committed or
currently visible worktree artifacts. It updates the earlier July 4 report after new
work landed on `codex/nrp-extensions`.

## Branch and Worktree State

| branch | head | July 4 commits | status |
|---|---|---:|---|
| `main` | `d7cc0da` (`Add July 4 status report`) | 1 | Clean and aligned with `origin/main` before this update. |
| `codex/nrp-skills` | `73a9b63` (`Add repo-local skills docs and PyYAML to dev shell`) | 0 | Local branch still points at the pre-report skills commit. |
| `codex/nrp-extensions` | `5b27f07` (`Advance NRP extension milestones`) | 4 | Separate local worktree, aligned with `origin/codex/nrp-extensions`, with additional uncommitted changes present. |

Visible worktrees:

- Primary checkout: `main`.
- Secondary checkout: `codex/nrp-extensions`.

## July 4 Commit Timeline

### `main`

- `d7cc0da` at 00:02 PDT - `Add July 4 status report`
  - Added the first version of this daily report.
  - At that time no July 4 implementation, experiment, benchmark, or test commits
    were visible before the report commit.

### `codex/nrp-extensions`

- `c424db1` at 10:44 PDT - `Add repo-local skills to AGENTS guidance`
  - Updated `AGENTS.md`.
  - Added repo-local skill docs and agent routing files:
    `skills/nrp-experiment-reporting/`, `skills/nrp-path-cache-and-gather/`, and
    `skills/nrp-torch-proxy-workflow/`.

- `342d85e` at 10:53 PDT - `Add open-top-box occluder fixture for E3 proxy tests`
  - Advanced E3 light-aware sampling evidence with an `open_top_box` fixture.
  - Key files changed: `nrp/toy_tracer.py`, `nrp/train.py`,
    `examples/light_aware_proxy_ab.py`, and `tests/test_light_aware_sampling.py`.
  - Docs updated: `docs/performance.md`, `docs/pipeline-feasibility.md`, and
    `docs/paper-mapping.md`.

- `5ae93a1` at 10:56 PDT - `Add extensions handoff status`
  - Added `docs/status/2026-07-04-extensions-handoff.md`.
  - The handoff documents extension status, verification commands, current
    evidence artifacts under `out/`, and recommended next work.

- `5b27f07` at 12:18 PDT - `Advance NRP extension milestones`
  - Advanced multiple extension slices and updated the handoff.
  - Key docs changed: `docs/paper-mapping.md`, `docs/performance.md`,
    `docs/pipeline-feasibility.md`, and
    `docs/status/2026-07-04-extensions-handoff.md`.
  - Key examples changed: `examples/out_of_core.py`,
    `examples/production_controls.py`, and `examples/textured_quad_fit.py`.
  - Key torch backend files changed: `nrp/torch_backend/model.py`,
    `nrp/torch_backend/relight.py`, `nrp/torch_backend/sampling.py`, and
    `nrp/torch_backend/train.py`.
  - Key tests changed: `tests/test_out_of_core.py`, `tests/test_texture_fit.py`,
    and `tests/test_torch_backend.py`.

## Extension Work Summarized From Committed Artifacts

The extension branch's committed handoff reports the following verified or advanced
slices:

- E2 dynamic geometry: one-bounce primary-visibility invalidation and image-space
  repair baseline remain present, with multi-bounce invalidation and TorchNRP
  fine-tuning still open.
- E3 light-aware sampling: toy `open_top_box` fixture reports a guided in-region
  PSNR gain of 10.10 dB with no fixed open-region regression in the documented A/B.
- E4 richer light models: learned texture-embedding proxy scaling and first-class
  2x2 textured-quad TorchNRP smoke are documented as complete at toy scale.
- E5 out-of-core: sharded cache, streamed target table, streamed image-proxy
  optimizer, and tiled inference are present; streamed TorchNRP optimizer training
  and a larger Mitsuba/RSS report remain open.
- E6 exported runtime: TorchScript-shaped runtime and CPU resolution sweep are
  present; WebGPU or engine-backend matrix, MPS timing, and GUI viewer evidence
  remain open.
- E7 generative loop: deterministic synthesized target/provenance fixture is
  present; high-quality proxy run and external or hand-authored image target remain
  open.
- E8 production controls: committed state includes gather-time light linking,
  linear attenuation, one binary linking toggle proxy, and one learned linear
  attenuation proxy.
- E9 final-frame quality: preview/draft/final plumbing and toy residual-validity
  trust verdict are present; production-scale final-frame trust remains open.
- E1 animated lights/camera: animated-light and camera-keyframe cache harnesses are
  present; a single proxy conditioned on time/camera inputs remains open.

## Current Uncommitted Worktree State

The primary `main` checkout was clean before this report update.

The `codex/nrp-extensions` worktree had additional uncommitted changes after
`5b27f07`:

- Docs modified: `docs/paper-mapping.md`, `docs/performance.md`,
  `docs/pipeline-feasibility.md`, and
  `docs/status/2026-07-04-extensions-handoff.md`.
- Code modified: `examples/production_controls.py`.
- Tests modified: `tests/test_production_controls.py`.
- Diff size at inspection time: 6 files changed, 219 insertions and 42 deletions.
- No untracked files were visible in that worktree.

Those uncommitted changes appear to broaden E8 production-control evidence from the
committed binary/linear-control baseline to toy-scale parameterized controls:
soft mask-basis proxying and quadratic attenuation proxying. Because they are
uncommitted and this reporting run did not execute the relevant example or tests,
this report records them as in-progress worktree evidence, not merged or independently
verified results.

## Verification Evidence

Commands used as evidence for this report:

- `git status --short --branch`
- `git branch --all --verbose --no-abbrev`
- `git worktree list --porcelain`
- `git log --all --since='2026-07-04 00:00:00 -0700' --date=iso-strict --pretty=...`
- `git log --all --since='2026-07-04 00:00:00 -0700' --date=iso-strict --pretty=fuller --stat`
- `git show --stat --oneline --decorate --name-status <commit>`
- `git -C <extension-worktree> status --short --branch`
- `git -C <extension-worktree> diff --stat`
- `git -C <extension-worktree> diff -- <selected docs/tests/examples>`
- `git -C <extension-worktree> ls-files --others --exclude-standard`
- `sed -n ... docs/status/2026-07-04-extensions-handoff.md`

Verification reported by the committed extension handoff after E3 occluder work:

- `ruff check .`: passed.
- `tests.test_light_aware_sampling`: passed, 4 tests.
- Full unit suite: passed, 191 tests, 16 skipped.
- Pipeline claims audit: passed.
- Path-hygiene scan over touched docs/code: passed.

This automation did not run unit tests, lint, training, export, or benchmark commands
itself. It therefore only reports test/benchmark results that are already recorded in
committed docs or visible worktree artifacts.

## Remaining Work

- Commit or discard the uncommitted E8 production-controls changes in
  `codex/nrp-extensions` after running the targeted checks.
- Continue the extension branch without merging it into `main` unless explicitly
  requested.
- Recommended next extension slice from the handoff: E5 streamed optimizer training;
  alternative higher-risk slice: E2 multi-bounce invalidation plus TorchNRP
  fine-tuning.
- Before promoting extension work, rerun the appropriate verification commands:
  `UV_CACHE_DIR=.uv-cache uv run ruff check .`,
  `UV_CACHE_DIR=.uv-cache uv run python -m unittest discover -s tests`, and the
  pipeline claims audit for `docs/pipeline-feasibility.md`.
