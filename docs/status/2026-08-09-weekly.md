# Weekly Status Report — Week Ending 2026-08-09

Scope: all commits reachable from local branches and remote-tracking refs dated
2026-08-03 through 2026-08-09 (America/Los_Angeles), every registered local
worktree, current branch state, and the committed status and experiment artifacts.
Inspected on `main` at `283b917`. This report does not merge feature branches.

## Week in Brief

No commits dated August 3–9 were found on any reachable local branch or
remote-tracking ref. The registered `main` and detached R1 worktrees are clean.
Accordingly, there are no code, documentation, test, or committed-artifact changes
to attribute to this reporting window.

## Branch and Worktree Evidence

| branch / worktree | work in the window | current evidence |
|---|---|---|
| `main` / `/Users/briangyss/src/nrp` | No commits. | Clean at `283b917` (`docs: add weekly status report for 2026-08-02`), aligned with `origin/main` before this report is committed. |
| `codex/implement-3d-hashgrid-r1` / detached `/Users/briangyss/.codex/worktrees/a521/nrp` | No commits. | Clean and detached at `fd2b7e8` (`Diagnose R1 variance and add tri-plane follow-up`). The branch and `origin/codex/implement-3d-hashgrid-r1` point to that same commit. |
| other refs | No commits. | `origin/worktree-scale-track` remains a remote-only historical ref at `cdf1498` (July 17); no other local branches or worktrees are registered. |

The window and state above were checked with `git log --all` constrained to the
reporting dates, `git branch --all --verbose --no-abbrev`, `git worktree list
--porcelain`, and `git status --short --branch` in both worktrees.

## Current Verified Project State

The most recent committed R1 evidence remains unchanged from the prior weekly report:
the direct world-space hashgrid did not meet its controlled per-seed parity gate, and
the parameter-matched tri-plane follow-up passed two of three seeds rather than all
three. `pixel2d` therefore remains the default; `world3d` and `world_triplane` remain
experiment-only. The underlying committed evidence is retained in
[`out/r1-worldgrid/report.json`](../../out/r1-worldgrid/report.json) and
[`out/r1-followup/report.json`](../../out/r1-followup/report.json), with the current
experiment tracker in [`representation-track.md`](../representation-track.md).

No new verification command or benchmark result is claimed for this week. The prior
report records the last known repository-wide checks: the unittest discovery suite and
Ruff both exited successfully, with optional-dependency skips and known environment
warnings noted there.

## Remaining Work

1. Execute the bounded five-seed, allocation, and coordinate-robustness ladder in
   [`2026-07-27-r1-next-experiments.md`](../plans/2026-07-27-r1-next-experiments.md),
   retaining the existing per-seed gate.
2. Do not advance R2 multi-camera through R6 Scene4D/WebGPU work until an R1
   representation passes that gate.
3. The scale-track items remain unchanged: improve rig per-light fidelity and
   additivity, bound real-scene dynamic-geometry recovery, and decide whether to fund
   the parked S7/S8 CUDA stretch rungs.

For the detailed prior-week evidence and command outcomes, see
[`2026-08-02-weekly.md`](2026-08-02-weekly.md).
