<!-- DRAFT — not published. Thread skeleton, 7 posts, each ≤ 280 chars.
Attach images/clips where marked. Before publishing: verify every number
against docs/performance.md / the cited out/ reports. -->

## Post 1 — hook

Can a path-cache-compressing MLP be the core of a real-time neural rendering
pipeline — games, animated film, VFX? I reimplemented Sancho et al. (EGSR 2026)
on a laptop, then ran 9 extensions to find out. Verdict + the failure that
mattered most 🧵

[attach: hero relight clip]

## Post 2 — runtime

The exported proxy runs as a real WebGPU compute shader in real Chrome: 2.4e-7
parity vs PyTorch, 580 fps @128², 107 fps @512². Same shader, same weights that
segfaulted a native Node binding 25/25 times. The defect was theirs — a
~150-trial bisection proved it.

[attach: browser viewer clip]

## Post 3 — scale

Out-of-core proof at 512×512/128spp — 94.2M path segments. Streamed training
peaks at 574 MB resident vs 3.35 GB monolithic (14.4× lower), bitwise-identical
loss curve at toy scale, 32.57 dB held-out at full scale. Single Apple Silicon
laptop, no CUDA.

[attach: streaming memory chart]

## Post 4 — the failure

The honest one: dynamic geometry broke it. Warm-started fine-tuning after a
geometry change: 25.20 dB vs a 44.88 dB full-retrain reference — 19.7 dB short
of target. Replay regularization → still 11.1 dB short. Settled structural
finding, and the program's most useful result.

[attach: recovery comparison figure]

## Post 5 — film/VFX ladder

Preview→draft→final quality ladder w/ per-config trust verdict, proven at 512²
on real Mitsuba caches: 33.76/35.72 dB preview/draft vs converged final, and
*exact* proxy+residual identity (max err 5.6e-17) at the approved frame. That's
the compositor-friendly contract.

[attach: tier comparison strip]

## Post 6 — verdict

The E10 verdict: all three audiences "partly viable," blockers named. Games —
frozen transport under dynamic geometry. Film — scale proof is cornell-box-only.
VFX — production light rigs unproven. Every claim traces to a committed JSON
report; an audit task enforces it.

## Post 7 — what's next

Next: a production track attacking exactly those blockers. A shared trunk (real
scene, streaming budget, perceptual gates, locked runtime baseline), then three
branches — each ending in a summit demo. Repo + docs in reply. 🏔️

[reply: repo link]
