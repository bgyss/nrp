<!-- DRAFT — not published. Follow-up to 2026-07-18-x.md. Thread skeleton,
9 posts, each ≤ 280 chars. Attach media where marked. Before publishing:
verify every number against docs/performance.md and docs/hardening-track.md. -->

## Post 1 — hook

Two weeks ago: found + fixed why 3/8 neural light proxies were silently
dead. That was rung 1 of a 6-rung hardening pass. All 6 done. Twice, I
caught the exact same bug hiding again in code I'd just written. Thread 🧵

## Post 2 — the retrain

Retrained the full 8-light rig with the fix: all 8 proxies now produce
real output, zero silent deaths. But the additivity gate still misses
preview-tier quality (SSIM 0.73 vs a 0.80 bar). Real progress, not a
clean win — reporting it as both.

## Post 3 — caught myself #1

Checked the art-direction recovery by hand instead of trusting my own
pass/fail script. One "recovered" light was just the starting guess —
output nonzero, but 5-6 orders of magnitude dimmer than the rest, under
my own detection threshold. 5/6 genuine, not the reported 6/6.

## Post 4 — caught myself #2

Built a "full retrain" benchmark for a real-scene dynamic-geometry test,
hand-rolled instead of reusing the fixed training path. Reproduced rung
1's exact zero-collapse mechanism in the new code. Same bug, same fix,
different file, two weeks later.

## Post 5 — honest negative #1

The 2 texture-conditioned light proxies can't be fixed by more training
time or a bigger network. Their texture is flattened into 200 raw
numbers, no spatial structure — every pixel query gets a real positional
encoding, the light's texture doesn't. Diagnosed, not just reported.

## Post 6 — honest negative #2

A toy-scale dynamic-geometry fix that looked promising 2 months ago does
NOT transfer to a real re-traced scene. Neither repair strategy meets the
1dB recovery target — closest misses by 14.5dB, the other by 40+dB. This
confirms the blocker is real, it doesn't soften it.

## Post 7 — what shipped clean

Shot storage: 1.17x raw frame size → 0.59x, just by keeping exact pixels
for a subset of "approval" frames only. Zero quality cost on any frame
tested.

## Post 8 — and

Light-rig compositing now runs on real WebGPU in real Chrome: ~1e-5 match
vs the CPU reference, 5x faster per light. Still short of feeling
real-time under a full multi-light edit, though.

## Post 9 — close

Full feasibility verdict re-issued against all of it — original kept
intact as history, not quietly edited. Repo in reply. 🔧

[reply: repo link]
