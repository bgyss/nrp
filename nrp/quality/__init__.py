"""Perceptual quality gates (production-track rung T3). See `nrp.quality.gate`.

Kept import-free so `python -m nrp.quality.gate` doesn't warn about the module
already being in sys.modules; import the API from `nrp.quality.gate` directly.
"""
